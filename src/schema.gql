scalar BigDecimal
scalar BigInt

type Payment {
  id: String!
  sender: String!
  token_address: String!
  token_symbol: String!
  amount_raw: BigInt!
  amount_decimal: BigDecimal!
  barcode: String!
  block: Int!
  timestamp: Int!
  type: String!
  space: String!
}

type Space {
  id: String!
  turbo_expiration_timestamp: Int!
  turbo_expiration_date: String!
}
